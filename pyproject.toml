[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
license = "Proprietary"
name = "sol-bot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "websockets>=10",
    "solana>=0.30",
]
